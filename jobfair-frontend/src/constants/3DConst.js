export const BOOTH_NAME_PREFIX = 'company';

export const GENERIC_BOOTH_LAYOUT_URL = `${window.location.origin}/booth-layout/generic_booth_layout.glb`;

export const IMAGE_PLANE_NAME = 'image-plane';

export const SHARPNESS_MARK = {
  0: 'normal',
  1: 'sharper',
  2: 'sharpest'
};

export const DEFAULT_HUMAN_MODEL_URL = `${window.location.origin}/human/Human.fbx`;
export const DEFAULT_IDLE_HUMAN_MODEL_URL = `${window.location.origin}/human/ModelIdle.fbx`;
export const DEFAULT_WALKING_HUMAN_MODEL_URL = `${window.location.origin}/human/WalkingModel.fbx`;
export const DEFAULT_HUMAN_MODEL_TEXTURE_URL = `${window.location.origin}/human/WalkingModel_8_LOWPOLY-COLORS_BaseColor.png`;
