import { createSlice } from '@reduxjs/toolkit';

const assignmentBreadcumbSlice = createSlice({
  name: 'assignmentBreadcumb',
  initialState: {},
  reducers: {}
});

export const assignmentBreadcumbActions = assignmentBreadcumbSlice.actions;
export default assignmentBreadcumbSlice.reducer;
